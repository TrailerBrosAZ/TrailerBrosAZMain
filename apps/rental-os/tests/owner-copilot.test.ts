import {describe,expect,it} from 'vitest';
import {interpretOwnerCommand,windowsOverlap} from '../src/shared/ownerCopilot.js';
const now=new Date('2026-07-16T12:00:00-07:00');
describe('owner copilot deterministic interpreter',()=>{it('prepares a Marketplace blackout without executing it',()=>{const draft=interpretOwnerCommand('Block the trailer Friday at 8 AM through Sunday at 6 PM for a Marketplace rental.',now);expect(draft).toMatchObject({intent:'CREATE_BLACKOUT',mode:'CONFIRMATION_REQUIRED',pickupAt:'2026-07-17T08:00',returnAt:'2026-07-19T18:00',source:'FACEBOOK_MARKETPLACE'});});it('prepares an external pickup draft with explicit missing customer data',()=>{const draft=interpretOwnerCommand('Create an external booking Friday at 8 AM through Sunday at 6 PM, customer pickup, no delivery.',now);expect(draft.intent).toBe('CREATE_EXTERNAL_BOOKING');expect(draft.fulfillment).toBe('CUSTOMER_PICKUP');expect(draft.missing).toContain('customer full name');});it.each([['What needs my attention today?','READ_ATTENTION'],['Show bookings that still need an agreement or pickup inspection.','READ_REQUIREMENTS'],['Give me a business summary for this month.','READ_ANALYTICS']])('returns deterministic read intent for %s',(command,intent)=>expect(interpretOwnerCommand(command,now)).toMatchObject({intent,mode:'READ_ONLY'}));it('refuses unsupported requests',()=>expect(interpretOwnerCommand('Refund every customer now',now).mode).toBe('UNSUPPORTED'));it('detects authoritative-window conflicts',()=>expect(windowsOverlap('2026-07-17T08:00','2026-07-19T18:00','2026-07-18T10:00','2026-07-18T12:00')).toBe(true));});
