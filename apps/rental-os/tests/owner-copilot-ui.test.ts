import {readFileSync} from 'node:fs';
import {describe,expect,it} from 'vitest';
const source=readFileSync(new URL('../src/client/OwnerCopilot.tsx',import.meta.url),'utf8');
describe('Owner Copilot UI safety contract',()=>{it('labels deterministic assistance and external transfer status',()=>{expect(source).toContain('DETERMINISTIC OWNER ASSISTANCE');expect(source).toContain('Not AI · no external data transfer')});it('requires an explicit confirmation control for mutations',()=>{expect(source).toContain('Confirm owner action');expect(source).toContain('No write occurs until you confirm')});it('provides direct reservation and intent navigation',()=>{expect(source).toContain('onReservation(item.id)');expect(source).toContain('onIntent(intent.id)')});it('renders priority, help, refund, availability, and playbook surfaces',()=>{for(const marker of ['TODAY’S PRIORITIES','Operational playbooks','refund-helper','availability-answer','PlaybookDialog'])expect(source).toContain(marker)});});
